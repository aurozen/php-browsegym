! function(t) {
    var e, i = t.event;
    i.special.smartresize = {
        setup: function() {
            t(this).bind("resize", i.special.smartresize.handler)
        },
        teardown: function() {
            t(this).unbind("resize", i.special.smartresize.handler)
        },
        handler: function(t, i) {
            var r = this,
                n = arguments;
            t.type = "smartresize", e && clearTimeout(e), e = setTimeout(function() {
                jQuery.event.handle.apply(r, n)
            }, "execAsap" === i ? 0 : 100)
        }
    }, t.fn.smartresize = function(t) {
        return t ? this.bind("smartresize", t) : this.trigger("smartresize", ["execAsap"])
    }
}(jQuery),
function(t) {
    var e = t(window),
        i = e.height();
    e.resize(function() {
        i = e.height()
    }), t.fn.parallax = function(r, n, a) {
        function o() {
            var a = e.scrollTop();
            p.each(function() {
                var e = t(this),
                    o = e.offset().top;
                o + s(e) < a || o > a + i || p.css("backgroundPosition", r + " " + Math.round((c - a) * n) + "px")
            })
        }
        var s, c, p = t(this);
        p.each(function() {
            c = p.offset().top
        }), s = a ? function(t) {
            return t.outerHeight(!0)
        } : function(t) {
            return t.height()
        }, (arguments.length < 1 || null === r) && (r = "50%"), (arguments.length < 2 || null === n) && (n = .1), (arguments.length < 3 || null === a) && (a = !0), e.bind("scroll", o).resize(o), o()
    }
}(jQuery),
function(t) {
    "use strict";
    t.fn.fitVids = function(e) {
        var i = {
                customSelector: null
            },
            r = document.createElement("div"),
            n = document.getElementsByTagName("base")[0] || document.getElementsByTagName("script")[0];
        return r.className = "fit-vids-style", r.innerHTML = "&shy;<style>               .fluid-width-video-wrapper {                 width: 100%;                              position: relative;                       padding: 0;                            }                                                                                   .fluid-width-video-wrapper iframe,        .fluid-width-video-wrapper object,        .fluid-width-video-wrapper embed {           position: absolute;                       top: 0;                                   left: 0;                                  width: 100%;                              height: 100%;                          }                                       </style>", n.parentNode.insertBefore(r, n), e && t.extend(i, e), this.each(function() {
            var e = ["iframe[src*='player.vimeo.com']", "iframe[src*='www.youtube.com']", "iframe[src*='www.kickstarter.com']", "object", "embed"];
            i.customSelector && e.push(i.customSelector), t(this).find(e.join(",")).each(function() {
                var e = t(this);
                if (!("embed" === this.tagName.toLowerCase() && e.parent("object").length || e.parent(".fluid-width-video-wrapper").length)) {
                    var i = ("object" === this.tagName.toLowerCase() || e.attr("height") && !isNaN(parseInt(e.attr("height"), 10)) ? parseInt(e.attr("height"), 10) : e.height()) / (isNaN(parseInt(e.attr("width"), 10)) ? e.width() : parseInt(e.attr("width"), 10));
                    if (!e.attr("id")) {
                        var r = "fitvid" + Math.floor(999999 * Math.random());
                        e.attr("id", r)
                    }
                    e.wrap('<div class="fluid-width-video-wrapper"></div>').parent(".fluid-width-video-wrapper").css("padding-top", 100 * i + "%"), e.removeAttr("height").removeAttr("width")
                }
            })
        })
    }
}(jQuery),
function(t) {
    var e = {
            topSpacing: 0,
            bottomSpacing: 0,
            className: "is-sticky",
            wrapperClassName: "sticky-wrapper",
            center: !1,
            getWidthFrom: ""
        },
        i = t(window),
        r = t(document),
        n = [],
        a = i.height(),
        o = function() {
            for (var e = i.scrollTop(), o = r.height(), s = o - a, c = e > s ? s - e : 0, p = 0; p < n.length; p++) {
                var u = n[p];
                if (e <= u.stickyWrapper.offset().top - u.topSpacing - c) null !== u.currentTop && (u.stickyElement.css("position", "").css("top", ""), u.stickyElement.parent().removeClass(u.className), u.currentTop = null);
                else {
                    var h = o - u.stickyElement.outerHeight() - u.topSpacing - u.bottomSpacing - e - c;
                    h < 0 ? h += u.topSpacing : h = u.topSpacing, u.currentTop != h && (u.stickyElement.css("position", "fixed").css("top", h), void 0 !== u.getWidthFrom && u.stickyElement.css("width", t(u.getWidthFrom).width()), u.stickyElement.parent().addClass(u.className), u.currentTop = h)
                }
            }
        },
        s = function() {
            a = i.height()
        },
        c = {
            init: function(i) {
                var r = t.extend(e, i);
                return this.each(function() {
                    var e = t(this),
                        i = e.attr("id"),
                        a = t("<div></div>").attr("id", i + "-sticky-wrapper").addClass(r.wrapperClassName);
                    e.wrapAll(a), r.center && e.parent().css({
                        width: e.outerWidth(),
                        marginLeft: "auto",
                        marginRight: "auto"
                    }), "right" == e.css("float") && e.css({
                        float: "none"
                    }).parent().css({
                        float: "right"
                    });
                    var o = e.parent();
                    o.css("height", e.outerHeight()), n.push({
                        topSpacing: r.topSpacing,
                        bottomSpacing: r.bottomSpacing,
                        stickyElement: e,
                        currentTop: null,
                        stickyWrapper: o,
                        className: r.className,
                        getWidthFrom: r.getWidthFrom
                    })
                })
            },
            update: o
        };
    window.addEventListener ? (window.addEventListener("scroll", o, !1), window.addEventListener("resize", s, !1)) : window.attachEvent && (window.attachEvent("onscroll", o), window.attachEvent("onresize", s)), t.fn.sticky = function(e) {
        return c[e] ? c[e].apply(this, Array.prototype.slice.call(arguments, 1)) : "object" != typeof e && e ? void t.error("Method " + e + " does not exist on jQuery.sticky") : c.init.apply(this, arguments)
    }, t(function() {
        setTimeout(o, 0)
    })
}(jQuery),
function(t) {
    var e = function(t) {
            return t.split("").reverse().join("")
        },
        i = {
            numberStep: function(e, i) {
                var r = Math.floor(e);
                t(i.elem).text(r)
            }
        },
        r = function(t) {
            var e = t.elem;
            e.nodeType && e.parentNode && ((e = e._animateNumberSetter) || (e = i.numberStep), e(t.now, t))
        };
    t.Tween && t.Tween.propHooks ? t.Tween.propHooks.number = {
        set: r
    } : t.fx.step.number = r, t.animateNumber = {
        numberStepFactories: {
            append: function(e) {
                return function(i, r) {
                    var n = Math.floor(i);
                    t(r.elem).prop("number", i).text(n + e)
                }
            },
            separator: function(i, r) {
                return i = i || " ", r = r || 3,
                    function(n, a) {
                        var o = Math.floor(n).toString(),
                            s = t(a.elem);
                        if (o.length > r) {
                            for (var c, p, u, h = o, l = r, d = h.split("").reverse(), m = (o = [], 0), f = Math.ceil(h.length / l); m < f; m++) {
                                for (c = "", u = 0; u < l && (p = m * l + u) !== h.length; u++) c += d[p];
                                o.push(c)
                            }
                            h = o.length - 1, l = e(o[h]), o[h] = e(parseInt(l, 10).toString()), o = o.join(i), o = e(o)
                        }
                        s.prop("number", n).text(o)
                    }
            }
        }
    }, t.fn.animateNumber = function() {
        for (var e = arguments[0], r = t.extend({}, i, e), n = t(this), a = [r], o = 1, s = arguments.length; o < s; o++) a.push(arguments[o]);
        if (e.numberStep) {
            var c = this.each(function() {
                    this._animateNumberSetter = e.numberStep
                }),
                p = r.complete;
            r.complete = function() {
                c.each(function() {
                    delete this._animateNumberSetter
                }), p && p.apply(this, arguments)
            }
        }
        return n.animate.apply(n, a)
    }
}(jQuery);